# Changelog

## 0.1.0

### Minor Changes

- Initial bootstrap of the Aiand provider: OpenAI-compatible chat completions at `https://api.aiand.com/v1` with `Bearer` API-key auth, live `/v1/models` discovery plus a bundled fallback catalog, streaming text/reasoning/tool-call projection, per-model reasoning-effort and context-window controls, and opt-in ghost-text inline suggestions.
